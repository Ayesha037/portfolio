# Portfolio (Vite + React)

This is a minimal Vite + React project to run the provided `Portfolio` component.

Quick start (Windows PowerShell):

```powershell
npm install
npm run dev
```

Then open the local dev server URL shown in the terminal.
